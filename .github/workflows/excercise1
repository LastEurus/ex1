//Java program to merge Fibonacci series numbers in arrays 

package tamrin1;

import java.util.Arrays;
import java.util.Scanner;

public class Array {
	public static void main(String[] args) {
		System.out.println("enter length of fib arrays:");
		Scanner scanner = new Scanner(System.in);
		int length = scanner.nextInt();
		System.out.println("length is : " + length);
		int[] series = new int[length];
		series[0] = 0;
		series[1] = 1;
//Recursive Function for Fib Series numbers
		for (int i = 2; i < length; i++) {
			series[i] = series[i - 1] + series[i - 2];
		}
		System.out.print(Arrays.toString(series));
		scanner.close();
	}
}
//Written by SMHHashemi 
